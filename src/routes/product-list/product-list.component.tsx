import { ProductListContainer } from "./product-list.styles";

const ProductList = () => {
  return (
    <ProductListContainer>
      List of added polishes
    </ProductListContainer>
  );
};

export default ProductList;
